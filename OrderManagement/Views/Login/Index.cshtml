@{
    Layout = null;
}
@*@Styles.Render("~/Content/css")
@Scripts.Render("~/bundles/bootstrap")
<script src="~/Scripts/bootstrapValidator.js"></script>
<link href="~/Content/bootstrapValidator.css" rel="stylesheet" />*@

@model OrderManagement.Models.LogOnModel
@using (Html.BeginForm("Login", "Login", FormMethod.Post))
{
    @*@Html.TextBoxFor(m => m.UserName, new { @class = "balanceValue" })
    @Html.ValidationMessageFor(m => m.UserName, "请输入用户名！", new { style = "color: #f00" })
    @Html.PasswordFor(m => m.Password)
    @Html.ValidationMessageFor(m => m.Password, "请输入密码！", new { style = "color: #f00" })
    @Html.CheckBoxFor(m => m.RememberMe)
    @Html.LabelFor(m => m.RememberMe)
    @Html.ActionLink("忘记密码", "forgotpwd", null, new { @class = "rt", target = "_blank" })
    <input type="submit" value="登陆微博" />*@
    
      <div class="container">
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                <div class="login-panel panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title">请登录</h3>
                    </div>
                    <div class="panel-body">
                        <form role="form">
                            <fieldset>
                                <div class="form-group">
                                    <input class="form-control" placeholder="请输入用户名" name="user" type="text" >
                                </div>
                                <div class="form-group">
                                    <input class="form-control" placeholder="请输入密码" name="password" type="password" value="">
                                </div>
                                <div class="checkbox">
                                    <label>
                                        <input name="remember" type="checkbox" value="记住密码">记住密码
                                    </label>
                                </div>
                                <input type="submit" value="登录系统" class="btn btn-lg btn-success btn-block" />
                                @*<a href="index" class="btn btn-lg btn-success btn-block">Login</a>*@
                            </fieldset>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
}  


